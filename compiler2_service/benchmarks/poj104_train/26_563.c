#define NUM_ITER 1320939

#include <header.h>

int main_bench()
{ char s[100];
	int i;
	gets(s);
	for(i=0;i<strlen(s);i++){
		if(s[i]!=' '||s[i]==' '&&s[i-1]!=' ')
		my_printf("%c",s[i]);
		else
	
		continue;
		}
}

